services:
  - type: web
    name: realestate-site
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: LEADS_FILE
        value: /tmp/leads.ndjson
